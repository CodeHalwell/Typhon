[project]
name = "typhon-distributed-kv"
version = "0.1.0"
src = "src"
out = "build"

[python]
target = "3.13"

[emit]
class-default = "dataclass"
format = true

[strictness]
no-implicit-any = true
exhaustive-match = "error"
methods-in-class-body = "warn"
auto-gather = false

[env]
required = []

[dependencies]
