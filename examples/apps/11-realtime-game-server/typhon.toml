[project]
name = "typhon-realtime-game-server"
version = "0.1.0"
src = "src"
out = "build"

[python]
target = "3.13"

[emit]
class-default = "dataclass"
format = true

[strictness]
no-implicit-any = true
exhaustive-match = "error"
methods-in-class-body = "warn"
auto-gather = false

[env]
required = []

[dependencies]
fastapi = ">=0.111"
uvicorn = ">=0.30"
pydantic = ">=2.6"
