---
title: Typhon
description: A statically-typed, stricter superset of Python that compiles to clean CPython 3.13+ — with zero runtime dependency on the toolchain.
template: splash
hero:
  tagline: A statically-typed, stricter superset of Python that compiles to clean, readable CPython 3.13+ — with zero runtime dependency on the toolchain.
  image:
    file: ../../assets/logo-dark.svg
    alt: Typhon language logo
  actions:
    - text: Read the ethos
      link: /Typhon/introduction/ethos/
      icon: right-arrow
      variant: primary
    - text: Install tyc
      link: /Typhon/getting-started/installation/
      icon: external
    - text: GitHub
      link: https://github.com/CodeHalwell/Typhon
      icon: github
      variant: minimal
---

import { Card, CardGrid, LinkCard, Tabs, TabItem } from '@astrojs/starlight/components';

## The 30-second pitch

> Every `.ty` file emits valid, idiomatic `.py`. Not all `.py` is valid Typhon.

Typhon is what Python could look like if you started over with three decades of language-design hindsight, while still keeping every Python library you love. It is a *compile-to-Python* language in the same sense that TypeScript is a *compile-to-JavaScript* language: you write the stricter, safer, more ergonomic dialect, and the compiler hands you idiomatic CPython that runs everywhere Python runs — no Typhon runtime, no PyPI dependency, no special interpreter.

<Tabs syncKey="pitch">
  <TabItem label="Typhon">

```python
type LoadError = NotFound | Timeout

class NotFound:
    path: str
class Timeout:
    after_ms: int

def load(path: str) -> Result[str, LoadError]:
    try:
        with open(path) as f:
            return Ok(f.read())
    except FileNotFoundError:
        return Err(NotFound(path=path))

def main() -> None:
    match load("config.toml"):
        case Ok(body):
            print(body)
        case Err(NotFound(path)):
            print(f"missing: {path}")
        case Err(Timeout(after_ms)):
            print(f"timed out after {after_ms}ms")
```

  </TabItem>
  <TabItem label="Emitted Python">

```python
from dataclasses import dataclass
from typhon_runtime import Ok, Err

@dataclass(slots=True)
class NotFound:
    path: str

@dataclass(slots=True)
class Timeout:
    after_ms: int

LoadError = NotFound | Timeout

def load(path: str) -> Ok[str] | Err[LoadError]:
    try:
        with open(path) as f:
            return Ok(f.read())
    except FileNotFoundError:
        return Err(NotFound(path=path))

def main() -> None:
    match load("config.toml"):
        case Ok(body):
            print(body)
        case Err(NotFound(path)):
            print(f"missing: {path}")
        case Err(Timeout(after_ms)):
            print(f"timed out after {after_ms}ms")
```

  </TabItem>
</Tabs>

The match on `LoadError` is statically exhaustive — add a third variant to the union and every match site goes red until you handle it. That is, in one screenshot, the kind of safety Typhon brings to Python.

## What you get

<CardGrid>
  <Card title="Non-nullable by default" icon="approve-check">
    Plain `T` cannot hold `None`. Use `T?` when you mean optional. Flow narrowing on `is None`, `is not None`, `isinstance`, `guard`, and early returns.
  </Card>
  <Card title="No implicit Any" icon="warning">
    Untyped values are a hard error outside `unsafe:` blocks. The strictness is stronger than TypeScript's `noImplicitAny`.
  </Card>
  <Card title="Errors as values" icon="puzzle">
    `Result[T, E]`, `Ok`, `Err`, and the `?` operator make failure visible in signatures — without giving up Python's exception bridge.
  </Card>
  <Card title="Sealed unions + exhaustive match" icon="setting">
    `type Shape = Circle | Rectangle | Triangle`. Add a variant; every `match` lights up red. Refactor with confidence.
  </Card>
  <Card title="Explicit async, smarter concurrency" icon="rocket">
    `gather:` lowers to `asyncio.TaskGroup` (the safe default). `go f(x)` spawns fire-and-forget through a strong-ref registry. No GC'd tasks.
  </Card>
  <Card title="Build-time configuration" icon="seti:config">
    `comptime let PORT: int = int(env("PORT", "8080"))` inlines at build time. Missing required env vars fail the build, not production.
  </Card>
  <Card title="Clean output, no runtime tax" icon="document">
    Emits idiomatic Python you can read, debug, and ship. The handful of helpers (`Result`, `lazy`, task registry) is generated locally as `typhon_runtime/`. No PyPI install.
  </Card>
  <Card title="One binary, full pipeline" icon="terminal">
    `tyc` is the compiler, formatter, language server, REPL, in-process VM, migrator, debugger launcher, and package-manager surface — sub-100ms incremental feedback via Salsa.
  </Card>
</CardGrid>

## Start here

<CardGrid stagger>
  <LinkCard title="The Ethos of Typhon" href="/Typhon/introduction/ethos/" description="Why this language exists, the values behind every design decision, and what kind of code Typhon is for." />
  <LinkCard title="Install in 60 seconds" href="/Typhon/getting-started/installation/" description="Build the tyc binary from source, scaffold a project, and run your first program." />
  <LinkCard title="The Five Rules" href="/Typhon/tour/five-rules/" description="If you remember nothing else from these docs, remember these five rules. Everything follows." />
  <LinkCard title="Language Tour" href="/Typhon/tour/values-bindings/" description="A guided walk through every feature with worked examples and emitted Python side by side." />
  <LinkCard title="Language Reference" href="/Typhon/reference/lexical/" description="The full surface of the language, syntax-form by syntax-form, for when you need the spec." />
  <LinkCard title="CLI Reference" href="/Typhon/cli/overview/" description="Every tyc subcommand and flag, with examples." />
</CardGrid>

## Status

Phases 0 through 3 of the [roadmap](/Typhon/project/roadmap/) are complete. **Phase 5 — interop and developer experience — shipped in [v0.1.6](https://github.com/CodeHalwell/Typhon/releases/tag/v0.1.6)**. **Phase 5.5 — constructor / method arity safety — shipped in [v0.2.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.2.0)** (`tyc::arg_count` on constructors and `impl` methods, cross-module shape propagation, `tyc::missing_field_init` audit for the `X.__new__(X)` bypass). **Phase 6 — Python-annoyances surface — shipped in [v0.3.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.3.0)** (`newtype`, `freeze let`, `pub`, `blocking_in_async` / `resource_not_managed` / `div_by_zero_literal` diagnostics, cross-platform install). **[v0.4.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.4.0)** is a type-checker correctness sweep on top of a third stress round (bool ⊆ int, subclass-constructor inheritance, dotted-attribute annotations, container-literal escape detection, De Morgan narrowing, triple-quoted round-trip). **[v0.5.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.5.0)** is the post-v0.4 roadmap sweep: Salsa cache shared across the LSP check path, `tyc debug` reads in full Typhon coordinates, dict-comp parallelisation, the `Type::TypeConstructor` foundation for higher-kinded types, `comptime` types-as-values, three new `tyc migrate` rewrites, and an opt-in PyPI corpus sweep harness. **[v0.5.1](https://github.com/CodeHalwell/Typhon/releases/tag/v0.5.1)** is a correctness + tooling point release: three emit-correctness fixes, a deep VS Code TextMate grammar audit closing ~18 miscolorings, and 22 new stdlib-only examples (47–68). **[v0.5.2](https://github.com/CodeHalwell/Typhon/releases/tag/v0.5.2)** is a correctness + documentation point release: two compiler fixes broadening the accepted surface (`<ident>?` propagation in value position, `set - set` / `frozenset - frozenset` arithmetic) and a ~40-file docs-site sweep adding side-by-side Typhon / Emitted-Python tabs to every complete-program example. **[v0.6.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.6.0)** is the apps-feedback minor release: a two-round campaign built ten multi-file production-shaped apps under `examples/apps/` (event-sourced banking, distributed KV, mini-compiler, search engine, GraphQL server, game ECS, trading engine, ML orchestrator, web crawler, task scheduler) and the release closes every issue from that campaign, plus three additive features (`Ok` / `Err` `Result`-combinator methods, `impl` on a sealed-union alias distributing to every variant, `tyc::stdlib_module_shadow` warning). **[v0.6.1](https://github.com/CodeHalwell/Typhon/releases/tag/v0.6.1)** is a polish release on top of v0.6.0: `let name: lowercase_type = …` highlights as a type annotation, and `.py.map` sidecars now live under `<out>/.sourcemaps/` mirroring the emitted Python tree. **[v0.7.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.7.0)** is the Round-3 apps-feedback carry-over: `pub *` wildcard re-export aggregation in `__init__.ty`, declare-only `let NAME: T` with arm-assignment, `with cm() as r:` inference from `__enter__` / `@contextmanager`, `await Callable[..., Awaitable[T]]` unwrapping, same-newtype-preserving arithmetic, cross-module generic method dispatch, ternary narrowing, `tyc::field_default_ordering`, and five new apps under `examples/apps/`. **[v0.7.1](https://github.com/CodeHalwell/Typhon/releases/tag/v0.7.1)** is a strict LSP bugfix point release. **[v0.8.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.8.0)** is the stress-test sweep release that closes **41 findings** spanning the type checker, VM, parser, lowering passes, diagnostics, and CLI. **[v0.8.1](https://github.com/CodeHalwell/Typhon/releases/tag/v0.8.1)** is a point release that adds a `partial` `InterfaceShape` marker so venv-introspected third-party classes don't false-positive `attribute_not_found`. **[v0.9.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.9.0)** is the stress-test cleanup release closing **32 findings** from a v0.8.1 stress sweep — the VM became usable as a daily-driver runner (`Result` combinators, `open()` write/append/binary modes, class patterns on built-ins, deep `freeze let`, comptime inlining, `lazy import`, `class!` exception fields, dataclass factories, `collections.deque` / `heapq` / `contextlib` / `pydantic` shims, multi-file projects, `@property` / `super()` / `@contextmanager`) and the type checker plugged silent-correctness gaps (read-view covariance, variant-to-parametric-union flow, `while True:` reachability + narrowing, `assert` narrowing, `*args` annotation policy, `extend list[T]` dispatch, exhaustive match on `T?`, `with`-chain error mismatch, PEP 695 `comptime let T: type` lowering). **[v0.9.1](https://github.com/CodeHalwell/Typhon/releases/tag/v0.9.1)** fixes four `tyc fmt` round-trip corruption modes and a `pub *` facade hole in sealed-union flow. **[v0.9.2](https://github.com/CodeHalwell/Typhon/releases/tag/v0.9.2)** seeds `class_parents` from `InterfaceShape.bases` during cross-module shape ingestion so `class! Sub(Foreign):` no longer false-positives `tyc::attribute_not_found` across module boundaries. **Current release: [v0.10.0](https://github.com/CodeHalwell/Typhon/releases/tag/v0.10.0)** — VM completeness release closing the dunder-dispatch and builtins coverage gaps that prevented `tyc run` from being a drop-in for `tyc build && python` on real-world programs. Operator overloading and rich comparisons (`__add__` + reflected forms, `__eq__` / `__lt__` / …, `__str__` / `__repr__` / `__len__` / `__getitem__` / `__contains__`) dispatch on user instances. `yield` / `yield from` finally work via eager generator materialisation (capped at 1M items). `type(x)` returns a real type object so `type(x).__name__` and `type(x) == int` work. `@property` getters fire on attribute read and `@classmethod` binds `cls`. The long tail of missing builtins lands: `divmod`, `pow` (2- and 3-arg), `format`, `ascii`, `int(str, base)` (incl. base=0), full set algebra, missing string methods (`center`/`ljust`/`partition`/`removeprefix`/`expandtabs`/…), `json.dumps(indent=…)`, `time.perf_counter` / `process_time`, `max`/`min`/`list.sort` with `key=`/`reverse=`/`default=` kwargs, pydantic `model_validate` / `model_dump`. Type checker exhaustiveness fixes (`match` over `bool` / string-literal unions / fixed-arity tuples), augmented-assign now type-checks on scalar targets, and `tyc-emit` shaves heap allocations out of literal emission. Additive on the accepted surface. The minimum-viable language — non-null types, sealed unions, `Result`, dataclass emit — is shippable today.

Typhon targets CPython 3.13+ (and the free-threaded 3.13t/3.14t builds when you opt in). It is not a new runtime, not a Mojo-style separate language, and not a replacement for the Python you already write. It is the language you reach for when you want Python's libraries with Rust-flavoured static safety and ergonomics.
