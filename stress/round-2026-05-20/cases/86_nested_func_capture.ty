# Closures — nested function capturing let / mut.

def make_adder(n: int) -> Callable[[int], int]:
    def inner(x: int) -> int:
        return x + n
    return inner

def main() -> None:
    let add5: Callable[[int], int] = make_adder(5)
    print(add5(10))

if __name__ == "__main__":
    main()
