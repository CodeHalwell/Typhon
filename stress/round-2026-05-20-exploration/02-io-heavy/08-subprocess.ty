import subprocess

def main() -> None:
    let result: subprocess.CompletedProcess = subprocess.run(
        ["echo", "hello from subprocess"],
        capture_output=True,
        text=True,
    )
    print(f"exit={result.returncode}")
    print(f"out={result.stdout.strip()}")

if __name__ == "__main__":
    main()
