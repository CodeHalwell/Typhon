# aiohttp 3.14.1 — mixed; lenient on most constructors.
import aiohttp
async def fetch() -> None:
    let s: aiohttp.ClientSession = aiohttp.ClientSession()
    await s.close()
def main() -> None:
    print("ok")
