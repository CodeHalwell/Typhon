# numpy 2.4.6 — C-extension; signatures unavailable, must stay lenient.
import numpy
def main() -> None:
    let a = numpy.array([1, 2, 3])
    let z = numpy.zeros(5)
    let r = a.reshape(3, 1)
    print(z)
    print(r)
