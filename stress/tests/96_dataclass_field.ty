from dataclasses import field

class Bag:
    items: list[int] = field(default_factory=list)
    note: str = "default"

def main() -> None:
    let b: Bag = Bag()
    b.items.append(1)
    print(b.items)

if __name__ == "__main__":
    main()
