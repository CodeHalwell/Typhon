class Foo:
    x: int

    # method in class body (not impl) - should warn methods_in_class_body
    def get_x(self) -> int:
        return self.x

def main() -> None:
    let f: Foo = Foo(x=5)
    print(f.get_x())
