class Pt:
    x: int
    y: int

def f(p: Pt) -> int:
    match p:
        case Pt(x=0, y=0):
            return 0
        case Pt(x=x, y=y):
            return x + y

def main() -> None:
    print(f(Pt(x=3, y=4)))

if __name__ == "__main__":
    main()
