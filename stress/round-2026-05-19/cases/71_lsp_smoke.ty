class User:
    id: int
    name: str

impl User:
    def display(self) -> str:
        return f"#{self.id} {self.name}"

def main() -> None:
    let u: User = User(id=1, name="alice")
    print(u.display())

main()
