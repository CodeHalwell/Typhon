def main() -> None:
    let xs: list[int] = [1, 2, 3, 4, 5]
    let a, *rest = xs
    print(a, rest)

    let head, mid, tail = xs[0], xs[1:4], xs[-1]
    print(head, mid, tail)

    let nested: tuple[int, tuple[int, int]] = (1, (2, 3))
    let x, (y, z) = nested
    print(x, y, z)

main()
