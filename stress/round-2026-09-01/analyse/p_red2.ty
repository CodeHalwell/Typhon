def f(xs: list[int]) -> int:
    mut total: int = 0
    mut i: int = 0
    mut x: int = -1
    while i < 2:
        print("prev x:", x)
        for x in xs:
            total += x * 2
        i += 1
    return total

print(f([1, 2, 3]))
