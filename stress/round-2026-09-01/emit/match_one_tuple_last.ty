def f(v: object) -> str:
    match v:
        case [a, b]:
            return f"two:{a},{b}"
        case (x,):
            return f"one:{x}"
    return "other"

def main() -> None:
    print(f([1]), f(5), f([1, 2]), f("s"), f((7,)))

main()
