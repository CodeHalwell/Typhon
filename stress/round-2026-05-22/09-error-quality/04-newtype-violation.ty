# Should fire `tyc::newtype_violation`.

newtype UserId = int


def fetch_user(uid: UserId) -> str:
    return f"user#{int(uid)}"


def main() -> None:
    # Bare literal where UserId is required.
    print(fetch_user(42))


if __name__ == "__main__":
    main()
